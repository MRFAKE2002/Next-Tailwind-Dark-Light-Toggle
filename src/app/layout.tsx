//! Metadata
import type { Metadata } from "next";

//! CSS
import "./globals.css";

export const metadata: Metadata = {
  title: "Tailwind Dark Toggle",
  description: "Generated by create next and tailwindcss",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
